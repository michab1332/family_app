import { StyleSheet, SafeAreaView, Text } from "react-native";

const ChildrenScreen = () => {

    return (
        <SafeAreaView>
            <Text>Children Screen - Parent</Text>
        </SafeAreaView>
    );
}

const styles = StyleSheet.create({

});

export default ChildrenScreen;