import { StyleSheet, SafeAreaView, Text } from "react-native";

const HomeScreen = () => {

    return (
        <SafeAreaView>
            <Text>Home Screen - Parent</Text>
        </SafeAreaView>
    );
}

const styles = StyleSheet.create({

});

export default HomeScreen;